# 单句收藏系统 - 完整设计方案

## 🎯 设计理念

### 核心思路
**无感知收藏 + 工具栏提示** - 让用户专注于播放，需要时自然收藏

### 用户体验流程
```
播放句子 → 看到工具栏 → 点击收藏 → 继续学习
```

---

## 🎨 UI 设计

### 1. 收藏工具栏（播放器底部）

```
┌────────────────────────────────────────────┐
│  ✕  [当前播放的英文句子...]   ⭐          │
└────────────────────────────────────────────┘
```

**组成部分：**
- **取消按钮** (✕) - 关闭工具栏
- **句子文本** - 当前播放的英文句子（截断显示）
- **收藏按钮** (⭐) - 收藏/取消收藏

**状态：**
- **空心星** ⭐ - 未收藏，点击收藏
- **实心金星** ★ - 已收藏，点击取消

### 2. 首页收藏入口

```
┌─────────────────────────────────┐
│  HANKILAB 学习中心      ⭐ 5   │
└─────────────────────────────────┘
```

- 星星图标右上角显示收藏数量徽章
- 点击查看所有收藏的句子

### 3. 收藏列表页

```
┌─────────────────────────────────┐
│  ← 返回  │  我的收藏  │  5 句  │
├─────────────────────────────────┤
│  ┌───┐ ┌───┐ ┌───┐ ┌───┐      │
│  │ 1 │ │ 2 │ │ 3 │ │ 4 │      │
│  │短│ │句│ │子│ │显│  ...   │
│  │文│ │文│ │文│ │文│          │
│  └───┘ └───┘ └───┘ └───┘      │
└─────────────────────────────────┘
```

- 显示句子片段（前 30 个字符）
- 显示所属课程编号
- 点击直接播放该句子

---

## 📦 数据结构

### 收藏对象格式

```javascript
{
  id: "NCE1_0_5",              // 唯一标识：课本_课程索引_句子索引
  key: "NCE1",                 // 课本标识
  unitIdx: 0,                  // 课程索引
  lineIdx: 5,                  // 句子索引
  sentence: "Excuse me!",     // 英文句子
  translation: "对不起！",    // 中文翻译
  lessonTitle: "Lesson 1",    // 课时标题
  bookTitle: "新概念第一册"   // 课本名称
}
```

### 存储方式

```javascript
localStorage.setItem('nce_favorites', JSON.stringify(favorites));
```

---

## 🎹 功能细节

### 1. 工具栏显示逻辑

| 触发条件 | 行为 |
|---------|------|
| 开始播放句子 | 显示工具栏 |
| 切换句子 | 更新句子文本，保持显示 |
| 关闭播放器 | 隐藏工具栏 |
| 暂停超过 3 秒 | 隐藏工具栏 |

### 2. 收藏/取消收藏

**收藏操作：**
1. 点击工具栏的星星图标
2. 星星变金色（实心）
3. 更新本地存储
4. 更新首页徽章数量

**取消收藏：**
1. 点击工具栏的星星图标（已收藏状态）
2. 星星变空心
3. 更新本地存储
4. 更新首页徽章数量

### 3. 收藏列表交互

**点击收藏卡片：**
1. 如果课本不同 → 先切换到对应课本
2. 打开对应课程
3. 自动播放收藏的句子
4. 显示工具栏（带收藏状态）

---

## 🔧 技术实现

### 核心函数

```javascript
// 显示/隐藏工具栏
showFavoriteToolbar()
hideFavoriteToolbar()

// 切换收藏状态
toggleCurrentSentenceFavorite()

// 更新首页徽章
updateFavBadge()

// 渲染收藏列表
renderFavorites()
```

### 事件监听

```javascript
// 工具栏收藏按钮
favoriteToolbarAdd.click → toggleCurrentSentenceFavorite()

// 工具栏关闭按钮
favoriteToolbarClear.click → toggleCurrentSentenceFavorite() + hideFavoriteToolbar()

// 首页收藏入口
showFavorites.click → renderFavorites() + 显示收藏页

// 收藏列表项
favoriteGrid.click → openBook() + playLine()

// 句子高亮切换
highlight() → showFavoriteToolbar()
```

---

## 📱 响应式设计

### 桌面端
- 工具栏宽度：固定 912px（居中）
- 句子文本：完整显示（截断 + 省略号）
- 按钮尺寸：36px

### 手机端
- 工具栏宽度：100% - 24px
- 句子文本：略小字号
- 按钮尺寸：32px
- 底部安全区域适配

---

## 🎯 优势对比

| 原方案 | 新方案 |
|-------|-------|
| 右上角小按钮，不易发现 | 底部工具栏，视觉焦点 |
| 没有句子提示 | 显示当前句子 |
| 功能单一 | 收藏 + 关闭双重功能 |
| 状态不明显 | 空心/实心对比鲜明 |

---

## 🐛 边界情况处理

### 1. 无有效句子
```javascript
if (this.cur < 0 || !this.lines[this.cur]) {
  hideFavoriteToolbar();
  return;
}
```

### 2. 收藏列表为空
显示空状态提示：
```
暂无收藏
播放时点击 ⭐ 收藏喜欢的句子
```

### 3. 跨课本收藏
自动切换课本 → 打开课程 → 播放句子

### 4. 删除 LRC 文件
收藏仍然存在，但播放时可能无歌词显示

---

## 🔄 数据流

```
用户点击收藏
    ↓
更新 favorites 数组
    ↓
保存到 localStorage
    ↓
更新 UI（星星状态 + 徽章数量）
    ↓
如果在收藏页，刷新列表
```

---

## 📊 性能优化

### 1. 懒加载
- 收藏列表仅在点击时渲染
- 不预先加载所有收藏的音频

### 2. 缓存
- 收藏数据缓存在内存
- 只在变更时写入 localStorage

### 3. 渲染优化
- 收藏列表使用虚拟滚动（未来）
- 避免重复渲染

---

## 🚀 未来扩展

### 1. 导出功能
- 导出收藏句子为 PDF
- 导出为 Anki 卡片

### 2. 分类管理
- 创建收藏夹
- 添加标签

### 3. 统计功能
- 收藏数量统计
- 学习时长统计

### 4. 云同步
- 跨设备同步收藏
- 登录账号支持

---

## ✅ 测试清单

- [x] 收藏当前句子
- [x] 取消收藏
- [x] 收藏列表显示
- [x] 点击收藏播放
- [x] 跨课本收藏播放
- [x] 徽章数量更新
- [x] 空状态显示
- [x] 响应式布局
- [ ] 导出功能（待实现）
- [ ] 云同步（待实现）
